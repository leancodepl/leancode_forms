name: leancode_forms_example
description: A new Flutter project.
publish_to: "none"
version: 0.2.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  leancode_forms:
    path: ..
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  leancode_lint: ^6.0.0

flutter:
  uses-material-design: true
