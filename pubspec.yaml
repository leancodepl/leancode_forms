name: advanced_forms
description: A package for managing form state based on ChangeNotifier and ValueListenable.
version: 0.2.0
homepage: https://github.com/leancodepl/advanced_forms
repository: https://github.com/leancodepl/advanced_forms
issue_tracker: https://github.com/leancodepl/advanced_forms/issues
topics:
  - forms
  - validation
  - state-management

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  collection: ^1.17.1
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  meta: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  leancode_lint: ^15.0.0
