name: leancode_forms
description: A package for managing form state based on ValueNotifier and ChangeNotifier.
version: 0.2.0
homepage: https://github.com/leancodepl/leancode_forms.git

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  collection: ^1.17.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  leancode_lint: ^15.0.0
